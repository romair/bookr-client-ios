language: objective-c
xcode_project: bookr.xcodeproj
xcode_scheme: bookrTests
before_install:
    - brew update
    - brew uninstall xctool && brew install xctool
    - brew bundle
install:
    - bundle install
    - bundle exec pod install
script: bundle exec rake test